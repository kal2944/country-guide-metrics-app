import React from 'react';
import { Link } from 'react-router-dom';
import './detail.css';

const Detail = () => (
  <div>
    <h2 className="detali">Detail page</h2>
    <Link to="/">click me</Link>
  </div>
);

export default Detail;
